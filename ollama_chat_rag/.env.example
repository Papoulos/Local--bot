# The Ollama model to use for embeddings and default chat.
# This is now mainly a fallback, as model names are defined in config.json
OLLAMA_MODEL=mistral

# The API key required to access the API endpoints.
# Choose a strong, secret key.
API_KEY="change_this_secret_key"
